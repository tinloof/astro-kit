# @tinloof/astro-prefetch

## 0.1.0

### Minor Changes

- a6c2331: Initial release: Next.js-style prefetching for Astro's ClientRouter — in-memory page cache with TTL + LRU, Intent-prioritized scheduler, tap/hover/viewport/load strategies plus a cursor-trajectory "proximity" predictor, navigation loader override (zero-network swaps, in-flight joins, miss population), and opt-in debug badge + overlay.
